# Core runtime dependencies for AI Log Analyzer.
#
# Install:            pip install -r requirements.txt
# Dev tooling:         pip install -e ".[dev]"
# Optional FastAPI:    pip install -e ".[api]"
#
# These versions are also declared in pyproject.toml under
# [project.dependencies]; keep the two in sync.

httpx>=0.27,<1.0
pydantic>=2.7,<3.0
PyYAML>=6.0,<7.0
typer>=0.12,<1.0
rich>=13.7,<14.0
diskcache>=5.6,<6.0
chromadb>=0.5,<1.0
